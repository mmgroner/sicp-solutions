# 3.63

Louis' version will require more computation because every time that the function recursively calls `sqrt-stream x` a new stream is created and all the previous guess must be re-evaluated. 

In our current implementation, the stream of guesses is bound to the `guesses` variable, and previous guesses are memoized. 

Without memoization, we would not have this benefit.
