# 3.66

The stream will lean heavily to producing lists which begin with the earlier elements of `s`. The reason for this is due to the way `interleave` works. `interleave` will alternate between the earlier and later streams, but as the streams continue to deepen recursively, `interleave` will still be alternating with all the earlier ones. 
