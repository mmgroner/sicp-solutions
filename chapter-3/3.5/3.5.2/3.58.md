# 3.58

I was not able to even try to figure thi out -- it requires too much mathematical knowledge for me at the moment.

Apparently, it returns the floating-point value of dividing `num` by `den` according to a specified `radix` (base number).

`(expand 1 7 10)` = `(/ 1 7)` = `(1 4 2 8 5 7 1 4 2 8)` = `0.142857143`
`(expand 3 8 10)` = `(/ 3 8)` = `(3 7 5 0 0 0 0 0 0 0)` = `0.375`
